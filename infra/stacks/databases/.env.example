# Database Configuration

# Redis Password (usado diretamente, não é secret)
REDIS_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# pgAdmin
PGADMIN_EMAIL=admin@authbrasil.com.br

# IMPORTANTE: As senhas do PostgreSQL e pgAdmin são gerenciadas via Docker Secrets
# Criar secrets antes do deploy:
# echo "sua_senha_postgres" | docker secret create db_password -
# echo "sua_senha_pgadmin" | docker secret create pgadmin_password -
