# AuthBrasil CNPJ - Production Environment

# Database (senha deve ser a mesma do secret db_password)
DB_PASSWORD=SENHA_DO_SECRET_DB_PASSWORD

# Redis (mesma senha configurada na stack databases)
REDIS_PASSWORD=SENHA_DO_REDIS

# Security (gerar com: openssl rand -hex 32)
SECRET_KEY=CHANGE_THIS_TO_SECURE_RANDOM_STRING_32_CHARS
JWT_SECRET_KEY=CHANGE_THIS_TO_SECURE_RANDOM_STRING_32_CHARS

# Stripe (usar chaves de produção)
STRIPE_SECRET_KEY=sk_live_your_production_key
STRIPE_PUBLISHABLE_KEY=pk_live_your_production_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# SendGrid
SENDGRID_API_KEY=SG.your_sendgrid_api_key

# IMPORTANTE: Em produção, migrar secrets sensíveis para Docker Secrets
# Exemplo:
# echo "sua_secret_key" | docker secret create app_secret_key -
# echo "seu_jwt_secret" | docker secret create app_jwt_secret -
