name: Deploy Backend

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v4

      - name: 🔄 Build & Deploy
        run: |
          set -e
          
          echo "📦 Copiando código para /root/backend..."
          sudo rm -rf /root/backend_backup
          if [ -d "/root/backend" ]; then
            sudo cp -r /root/backend /root/backend_backup
          fi
          
          # Copiar novo código
          sudo rsync -av --exclude='.git' --exclude='__pycache__' --exclude='*.pyc' backend/ /root/backend/
          
          echo "🏗️ Building API image..."
          cd /root/backend
          sudo docker build -t authbrasil/api:latest .
          
          echo "📊 Running migrations..."
          sudo docker run --rm --user root \
            --network network_public \
            -e DATABASE_URL=postgresql://authbrasil_user:${{ secrets.DB_PASSWORD }}@postgres:5432/authbrasil_cnpj \
            -e SECRET_KEY=${{ secrets.SECRET_KEY }} \
            -e JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY }} \
            -e REDIS_URL=redis://:${{ secrets.REDIS_PASSWORD }}@redis:6379/0 \
            -e STRIPE_SECRET_KEY=${{ secrets.STRIPE_SECRET_KEY }} \
            -e STRIPE_PUBLISHABLE_KEY=${{ secrets.STRIPE_PUBLISHABLE_KEY }} \
            -e STRIPE_WEBHOOK_SECRET=${{ secrets.STRIPE_WEBHOOK_SECRET }} \
            -e SENDGRID_API_KEY=${{ secrets.SENDGRID_API_KEY }} \
            authbrasil/api:latest python -m alembic upgrade head || echo "⚠️ Migrations já executadas"
          
          echo "🚀 Deploying stack..."
          sudo docker stack deploy -c /root/docker-compose.backend.yml authbrasil
          
          echo "🔄 Rolling update..."
          sleep 5
          sudo docker service update --force authbrasil_api
          
          echo "⏳ Aguardando service atualizar..."
          sleep 15
          
          echo "🧹 Limpando..."
          sudo docker container prune -f
          sudo docker image prune -f
          
          echo "✅ Backend deployed successfully!"
          echo "📊 Status:"
          sudo docker service ls
